<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>
 
<p>
  <strong>Notes:</strong>
  <%= @category.notes %>
</p>

<h2>Timelogs:</h2>
<%= render @category.timelogs %>

<%= link_to 'Back', categories_path %>
| <%= link_to 'Edit', edit_category_path(@category) %>
